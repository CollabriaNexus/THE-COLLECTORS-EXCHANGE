DATABASE_URL="postgresql://postgres:[PASSWORD]@db.rjyjblczxhxebtyvglnr.supabase.co:5432/postgres"
PORT=3000
JWT_SECRET="your-super-secret-luxury-key"
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key
# Optional JWT claim overrides for plugins/auth.js. Defaults are
# SUPABASE_JWT_ISSUER=<SUPABASE_URL>/auth/v1 and SUPABASE_JWT_AUDIENCE=authenticated.
# Set these ONLY if token verification starts failing with an iss/aud mismatch.
# SUPABASE_JWT_ISSUER=
# SUPABASE_JWT_AUDIENCE=
RAZORPAY_KEY_ID=rzp_live_xxxxxxxx
RAZORPAY_KEY_SECRET=your-razorpay-secret
# Separate from RAZORPAY_KEY_SECRET. Set this to the secret you typed into the
# Razorpay dashboard when creating the webhook that points at
# POST /api/checkout/razorpay-webhook. If unset, that route returns 503 instead
# of trusting an unverified payload.
RAZORPAY_WEBHOOK_SECRET=your-razorpay-webhook-secret
FRONTEND_URL=http://localhost:5173
SELLER_AGREEMENT_PDF_URL=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
GOOGLE_MERCHANT_KEY=base64-encoded-service-account-json
META_CATALOG_ACCESS_TOKEN=your-meta-system-user-access-token
TRACKING_URL=https://www.delhivery.com
